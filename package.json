{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest tsconfig-paths --silent --noStackTrace --runInBand",
    "test:verbose": "yarn test tsconfig-paths",
    "test:unit": "yarn test tsconfig-paths--watch -c jest-unit-config.ts",
    "test:integration": "yarn test tsconfig-paths --watch -c jest-integration-config.ts",
    "test:ci": "yarn test tsconfig-paths --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.22",
    "@types/validator": "^13.1.3",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "ts-node-dev": "^1.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "validator": "^13.5.2"
  }
}
